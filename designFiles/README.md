# Design Files

Planning docs, kept up to date as the plan evolves (chat context gets compacted over time — these files are the durable record). The actual Flutter project lives in `TerpinLift/`.

- `00_UX_DESIGN.md` — overall vibe, display philosophy (icon → plot → words), chart conventions (adaptive polynomial/moving-average trends), units system, soreness body map + lift muscle diagrams, nav structure, privacy/notification rules.
- `01_DATA_MODEL.md` — SQLite schema (tables, columns, migration notes).
- `02_SCREEN_home.md` / `03_SCREEN_lifts.md` / `04_SCREEN_quick_log.md` / `05_SCREEN_metrics.md` / `06_SCREEN_settings.md` / `09_SCREEN_cycle_detail.md` — one doc per screen.
- `07_SMART_TRENDS.md` — the layered plan for rule-based → RPE-weighted trend analysis (the "not-quite-AI" engine). No workout-planning logic — this only judges recovery/prediction, never picks exercises for you.
- `08_GLOSSARY_AND_SCIENCE.md` — plain-language source text for in-app "i" tooltips (RPE, e1RM, soreness, etc.) plus the sports-science reasoning behind each formula.
- `10_WORKOUT_PLANNER.md` — decision doc + status for a movement-pattern-based session builder (phase 1 implemented, phases 2-3 not started). Deliberately scoped as a sorter, not a scheduler, to stay consistent with the "never picks exercises for you" rule above.
- `11_SCREEN_cardio.md` — "light handling" of cardio (Run/Ruck/Bike/Rowing/Stairs/general) as a parallel backend to lifts, not bolted onto the reps/weight schema. Distance/duration/optional load, two independent goal gauges (distance, speed), a forked detail screen.
- `12_SCREEN_hiit.md` — a circuit/interval builder (rounds of lift/cardio slots, rest/transition timers, automatic-vs-manual advance) that saves into the existing lift/cardio backends rather than being its own logging system.
- `13_SCREEN_onboarding.md` — a two-page first-run survey (welcome, then gender/birth year/starting weight) shown once per profile via `AppRoot`; "Reset Demo Data" doubles as a way to preview/test it against the disposable demo database without ever touching personal data.
- `14_PLAY_STORE_RELEASE.md` — checklist/reference for the Play Store submission under the "Basener Studio" developer account: package name, release signing setup, store listing copy, and what's still manual (keystore, screenshots, feature graphic).

## Status
Core app is built and running (`TerpinLift/`): logging (93+ seeded lifts — barbell compounds including Olympic-style jerks/snatch, isolation accessories, bodyweight movements, named machines/cables, 7 cardio exercises (`11_SCREEN_cardio.md`) — + custom movements), sets/sessions with a **0-10 "reps left" effort field** (0 = true failure/"tried one more and couldn't," 9 and 10 both collapse to the RPE floor since past ~8 left the distinction stops meaning anything), a searchable exercise picker (defaults to pinned, typing searches the full library) used both for quick-log and for reassigning a logged session's exercise, per-session notes, edit/delete on any logged session, a quick "+" log-set button on Lift detail.

**Add/edit exercise (2026-07-16):** adding a new custom movement is a 3-step wizard (name+tags, muscle selection, optional goal+notes) — only name and picking at least one muscle are required, everything else skippable. Editing an existing exercise stays single-page. Movement-pattern tags are **not** user-editable from the app at all (backend/seed-data only — they only drive the Workout Planner's pattern-pool matching). Each exercise can carry **user-set fine-grained target muscles** (`MuscleSelectorSheet` — full front+back body-map tap targets, not just broad groups), falling back to curated defaults for older seeded lifts that haven't had this set. The old curated per-lift "Overview" cues are gone entirely, replaced by **free-text personal notes** the user writes themselves.

A **Workout Planner (phases 1-2)** — a "Plan a session" card on Home (dark when idle, turns red when a session's active) → Day Select (days from a seeded default template matching a real gym-plan PDF, most-overdue-first) → the Active Day page (elapsed timer, a soft per-slot progress bar, a notes field, one card per movement-pattern slot showing what's already been done, tap through to that pattern's readiness-sorted exercise pool → existing Lift Detail to actually log) → Abort/Complete. Viewing a **completed** session also exposes editable start/end times (2026-07-17) — a completed session's own span now drives the Metrics "Workout Duration" chart in preference to summing individual lift-log Track Time windows, which could badly undercount the real workout. A sorter, not a scheduler — logging works normally from anywhere whether or not a session is active, matches are derived live from same-day logged sessions rather than requiring an explicit completion step. Phase 3 (multiple saved templates, CSV import/export, a Workouts-tab day annotation) is scoped but not built, see `10_WORKOUT_PLANNER.md`.

Home/Lifts/Metrics/Settings screens, **pinned lifts** (push-pin toggle on Lift detail; drives the exercise picker's default list and a Lifts-list quick filter, since the seeded library is now too big to browse in full every time), a Workouts tab (Lifts screen) grouping every logged session by day with an RPE-intensity bar and Workout Planner day-grouping, a Filter/Sort pair on the Lifts tab (union-match pill filter, pinned-only toggle, alphabetical/most-used/push-pull/muscle-group/type/readiness sort), a Days tab (Metrics screen) grouping every logged bodyweight/steps/sleep/soreness/custom-metric entry by day with per-entry edit (soreness collapsed to one multi-region row per day), week rings, a muscle-group status icon row on Home (ready/resting/overreaching per category), a "good picks right now" primed-lifts row (greedy set-cover over primed muscles, max 5) under the Primed for Growth map, a **customizable Strength Trends section** on Home (edit-pencil picker for exactly which lifts chart and how many months of history, defaulting to pinned lifts), Training Split composition chart, a unified compass-derived readiness score (Lifts list + Lift detail, `ReadinessEngine`) with an always-visible confidence range, per-body-part soreness tracked via a tappable body-map diagram (`flutter_body_heatmap`, softened color scale) with the same muscle data reused for static per-lift "muscles worked" diagrams, a per-muscle "Primed for Growth" readiness map on Home, a bodyweight-aware e1RM path (`bodyweightAdjustedBestE1rm`) so assisted/weighted pull-ups stay comparable on one curve, Cycle detail screen (flow calendar + variability bar charts + a metric-overlay dropdown for sleep/steps/workout days/intensity/PRs/**custom metrics, female-gated**), lb/kg units toggle, a hide-weight-numbers toggle (masks read-only weight displays app-wide), backdatable quick-log entries, export/import (known gaps — see Known TBD below), and a synthetic test-data generator with real narrative shape (growth/plateau/slump/rebound, DOMS-timed soreness decay, cycle-linked dips, an assisted→bodyweight→weighted Pull Up arc, a plain-bodyweight Push Up rep climb, real Workout Planner sessions on ~half of gym days, synthetic progress photos, a demo "Mood" custom metric, and — since 2026-07-19 — cardio sessions for Run/Rowing Machine/Stairs with both distance improving and pace/duration tightening across the window, plus a distance and (where applicable) pace goal per exercise so the new goal gauges have something real to show) for reviewing trends without waiting on real data — rotates a curated ~11-lift routine subset rather than the full ~93-exercise library, so trend charts stay dense enough to read.

**Lift detail was substantially reworked (2026-07-15 to -17).** The near-term prediction box ("Next Heavy Set," renamed) now projects a weight at the rep count the user actually tends to go heavy at, from same-rep-count history only — no more converting between rep ranges via Epley, which was producing bad estimates on near-failure multi-rep sets. The trend chart ("Heavy Set History," renamed) plots literal per-session heaviest weight (never a formula), dot size encoding that set's rep count, with a dashed e1RM-shaped trend line re-centered onto the raw-weight scale rather than plotted on its own axis. **All decay/detraining-curve modeling was removed app-wide** — Predicted 1RM is now just the best of the last 3 sessions, no taper — per the user's call that it implied more scientific rigor than the underlying math actually supports at this level. The Goal gauge's custom-goal path is a full **log**, not a single overwritable value: multiple labeled targets over time, the most recent driving the gauge, older ones browsable/deletable via a history sheet, selectable per-lift alongside (or instead of) a published bodyweight-ratio standard via a Standard/My Goal toggle. The True Max line now also shows the rep count it was hit at.

**Metrics gained two new self-contained features (2026-07-17).** **Progress photos** — a date-stamped image log, shown as a day-stacked "pile" album (photos pinned to a shared bottom-left corner, rotated per photo, capped at 3 shown and always the most recent), tap a stack to swipe just that day or a toolbar icon to swipe every photo; delete and a download-via-share-sheet action on each. **The metric builder** — unlimited user-defined metrics (plain number / a 0-5 scale with a chosen icon — flame, star, dot, heart, bolt, moon, drop — / named classes with a "Mood" preset), each with its own trend chart and a once-vs-multiple-per-day setting per metric. **Every** metric card, built-in and custom alike (including Soreness and the derived Workout Duration chart), now has a notebook icon opening a focused single-metric history popup — deliberately not a jump to the Days tab, since browsing "every Steps reading ever" and "everything logged today" are different jobs.

**Home gained layout editing and a Checklist widget (2026-07-18).** An edit toggle in the AppBar turns every Home section into a drag-reorderable, swipe-to-remove list (`SliverReorderableList` + `Dismissible`, drag limited to a dedicated handle icon so it doesn't fight the swipe gesture), with a "+" menu while editing to add back anything hidden — order/visibility persists as one setting (`home_widget_order`, an ordered list of `HomeWidgetId`s; unset falls back to the original default order). **Checklist** is a new small fixed list of recurring reminders (distinct from a one-off todo list) — tap-to-check rows that strike through and reset automatically every calendar day (`TodoItem.lastCheckedDate == today`, no background job needed), a batch edit sheet matching the lift-log set-row editor's shape, and an optional per-item time that schedules a real daily-repeating local notification (`flutter_local_notifications`, inexact scheduling to avoid Android 12+'s heavier exact-alarm permission). See `02_SCREEN_home.md` "Layout editing" and "Checklist" for the full mechanics.

**Home's edit mode refined and a Metric Trend widget added (2026-07-19).** Bug fixes from on-device use: newly added widgets now insert at the top of the order (not bottom); a drag-proxy `Material` wrapper fixed a red-screen crash when dragging the Plan-a-session card (`SliverReorderableList`'s default proxy doesn't provide one, unlike `ReorderableListView`); reorder/remove/add now `setState` immediately after persisting instead of waiting on the next full data reload, fixing a visible "snap back then snap into place" lag. **Strength Trends** was split from one combined section into a **repeatable single-lift widget** — each card its own header/edit pencil, add as many as you want. **Metric Trend** is a new sibling repeatable widget for Steps/Sleep/Weight/Workout Duration/custom metrics, deliberately sharing its point-computation code with the Metrics screen (`MetricChartPoints`) rather than reimplementing it. Every widget type in the "+" add menu now shows a small icon next to its name. Checking off a Checklist item now silences *today's* reminder notification (reschedules starting tomorrow) rather than just leaving it firing; a 1-minute timer forces a Checklist rebuild if the app is left open across midnight. See `02_SCREEN_home.md` and `05_SCREEN_metrics.md`.

**Cardio backend (2026-07-19, revised same day).** A parallel `cardio_sessions`/`cardio_entries` table pair (distance/duration/optional resistance/effort) for Run/Ruck/Bike/Rowing/Stairs/general cardio — deliberately not forced through the lift reps/weight schema. `CardioDetailScreen` is a forked (not parametrized) counterpart to Lift Detail: a Distance History chart, two independent goal gauges (Distance and **Pace**, not speed — on-device feedback flipped this from the initial mph-based cut, since pace is what actually matters for running/rucking; `SingleGoalGauge` gained a `lowerIsBetter` mode so the same widget still works), history with edit. **Distance units are per-exercise, not app-wide** — `Exercise.cardioUnit` (miles/km/meters/floors, set via the pencil-edit sheet) is the display/canonical unit each exercise's entries and goals convert through; each logged entry can still be typed in a different (but convertible) unit via a per-entry dropdown. Floors is deliberately excluded from the mile/km/meter conversion family and gets no pace tracking. A shared `SimpleTimerSheet` (start/pause/reset, screen-awake via `wakelock_plus`, optional interval beep, no background survival) is reachable from both `LogLiftForm` and `LogCardioForm` next to the existing Calc button. Reachable via a "Cardio" quick-log tile; routing to the right detail screen (`exerciseDetailScreen`) is centralized so no nav call site can send a cardio exercise through the wrong screen. Deliberately **not** wired into the muscle-readiness/Primed-for-Growth engine — a scope boundary, not an oversight. See `11_SCREEN_cardio.md`.

**HIIT (2026-07-20).** A circuit builder (`HiitSetupScreen`) — rounds of lift/cardio exercises ("groups" of "slots," mirroring the Workouts tab's day-grouping visual language), each with a target (reps, AMRAP-within-a-time-cap, a fixed time, or a distance) and optional rest before the next slot or the next round; a global automatic-vs-manual toggle governs whether reaching a goal auto-advances or waits for a tap. `HiitActiveScreen` runs it one slot at a time behind a big `CountdownRing` (new widget, adapted from `WeekRings`' ring painter) doubling as a tappable button, a round-aware segmented progress bar up top, and Pause/Abort — pausing genuinely persists (not just an in-memory timer), so leaving the screen and coming back later through Home's HIIT card resumes exactly where it left off. `HiitReportScreen` closes it out: the app assumes every target was hit exactly as planned, so this is purely a correction pass (dropped weight, cut a run short, AMRAP reps counted after the fact) plus notes, and Save writes real `LiftSet`/`CardioEntry` rows into the *existing* lift/cardio backends — a HIIT workout is invisible to the data model once saved, just grouped for display in the Workouts tab (matched by date+exercise, same spirit as the Workout Planner's day-grouping). No saved/reusable routine templates yet (explicitly deferred) and no live weight editing mid-session (also deferred) — see `12_SCREEN_hiit.md` for the full design and its known gaps.

## Automated tests
`flutter test` covers the pure-logic services — no DB, no widgets, just known inputs against expected outputs (`TrendEngine.predictNextAtCharacteristicReps`/`predictedOneRepMax`, `StrengthStandards`' tier math, `BodyweightRepStandards`, `EffortDisplay`, `ReadinessEngine.toBars`/`readinessForExercise`/`suggestPrimedLifts`/`computePatternRecency`, `LiftSet`/`SessionWithSets` e1RM math including the bodyweight-adjusted path, `WorkoutPlanService.matchedSessions`/`progressFraction`). Deliberately scoped to what's cheap and stable to test — no widget/integration tests yet, no DB-backed repository tests (would need a fake/in-memory sqflite setup, not done). Run via `flutter test` from `TerpinLift/`. First real test coverage in the app; added specifically so the next time the smart-trends math gets touched, a regression shows up immediately instead of weeks later mid-workout.

## Known TBD (carry into next session)
- **HIIT's saved-routine templates and live mid-session weight editing** — both explicitly deferred by the user during the 2026-07-20 build ("get the main idea down first" / "we can skip live editing" for now). See `12_SCREEN_hiit.md` "Known gaps" for these plus the smaller v1 scope trims (no whole-workout elapsed clock, cardio-produced HIIT entries not yet shown in the Workouts tab since that tab is lift-only).
- **Export/Import doesn't cover several newer tables (including the two new HIIT ones), and progress-photo files aren't included at all** — see `06_SCREEN_settings.md` "Export data" known-gap note. Earmarked by the user (2026-07-17), not to be fixed opportunistically — revisit when Export/Import itself is the actual task.
- **Muscle status icons** — the user likes the per-category summary concept but isn't sold on the current form; wants something more holistic/navigational (possibly a text takeaway like "good day to push legs," on top of or instead of the icons). Explicitly not decided yet — see `07_SMART_TRENDS.md`.
- First real-gym-use pass done this round (equipment tags, reps-left framing, quick log-set button, Days tab, hide-weight, Lifts filter/sort, bodyweight-lift support), a second round (muscle-status icons, primed-lifts row, bulk exercise library), a third round (pinned lifts, Home trend customization, alphabetical/most-used sort), and a fourth round (soreness ordering bug + sub-splitting, primed-lifts scoring fix, first automated tests) — worth an on-device pass before the next round of feature work to catch what's still sticky.
- The Home trend picker's exercise checklist isn't searchable yet — see `02_SCREEN_home.md`.
- **Workout Planner phase 3** — multi-template switching (Settings), CSV import/export, and the Workouts-tab day annotation are scoped in `10_WORKOUT_PLANNER.md` but not built. One deliberately deferred open question there: retroactive completion (editing a past session to satisfy a slot after the fact) — revisit when phase 3's edit flow is actually being built.

## Still ahead
Personalized short-term sub-goals for the Strength Goal system were considered and **dropped, not deferred** — walking through it with the user, it turned out to duplicate what the near-term prediction box already does (near-term, progressive-overload, derived from recent sessions — that box has since been reworked and renamed to "Next Heavy Set," see `07_SMART_TRENDS.md`, but the "don't duplicate it" reasoning still holds). Fitbit integration (discussed, explicitly skipped for now — free/no-cost personal developer tier, OAuth2 + refresh-token flow is the real work, steps/sleep/HR all available, generous rate limits at single-user scale; revisit if/when the app takes on its first network dependency), cycle-phase correlation flag, recomposition flag. Also: embedded YouTube player vs. launch button, a box-plot-style per-workout weight-range chart, and a randomized-math confirmation for destructive actions (delete exercise, wipe/load test data) — all explicitly deferred, see `03_SCREEN_lifts.md`.
